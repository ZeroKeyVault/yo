{
  "name": "redrabbit-relay",
  "version": "1.0.0",
  "description": "RedRabbit encrypted relay server — stores only opaque AES-GCM blobs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
